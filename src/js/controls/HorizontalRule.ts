import StaticElement from "./StaticElement";

export default class HorizontalRule extends StaticElement {
    static config = {
        name: "Horizontal Rule",
        icon: "horizontalRule",
        tag: "hr",
        content: null,
        attrs: null
    }
}