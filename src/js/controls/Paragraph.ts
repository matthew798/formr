import StaticElement from "./StaticElement";

export default class Paragraph extends StaticElement {
    static config = {
        name: "Paragraph",
        icon: "paragraph",
        tag: "p"
    }
}